import { sql } from '@vercel/postgres';
import { useEffect, useState } from 'react';

export default function Home() {
    return (
        <div>
            {/* {array &&
                array.map((row) => (
                    <div key={row.id}>
                        {row.id} - {row.title}
                    </div>
                ))} */}
        </div>
    );
}
